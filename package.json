{
  "name": "rebook-tts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TTS service for rebook with provider-based synthesis.",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "start": "node dist/server.js",
    "dev": "tsc -w -p tsconfig.json"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "node-edge-tts": "^1.2.10"
  }
}
